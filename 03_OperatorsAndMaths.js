// JavaScript Questions for Practice (Basic Operators & Maths) 

// Arithmetic Operators: 

// I. Addition and Subtraction: 
// 1. Declare two variables, num1 and num2, and assign them numerical values. 

let num1 = 15;

let num2 = 10;

// 2. Use the addition and subtraction operators to find the sum and difference between num1 and num2. 

let addition = num1 + num2;

console.log(addition);

let subtraction = num1 - num2;

console.log(subtraction);

// II. Multiplication and Division: 
// 3. Declare two variables, value1 and value2, and assign them numerical values. 

let value1 = 20;

let value2 = 10;

// 4. Use the multiplication and division operators to calculate the product and quotient of value1 and value2. 

let multiplication = value1 * value2;

console.log(multiplication);

let division = value1 / value2;

console.log(division);

// Assignment Operators: 

// III. Increment and Decrement: 
// 5. Declare a variable count and assign it an initial value. 

let count = 1;

// 6. Use the increment (++) and decrement (‐‐) operators to modify the count variable. 

count++;

console.log(count);

count--;

console.log(count);

// IV. Compound Assignment: 
// 7. Declare a variable total and initialize it with a value. 

let total = 10;

// 8. Use compound assignment operators (+=, ‐=, *=, /=) to update the total variable. 

total += 10;

console.log(total);

total -= 10;

console.log(total);

total *= 10;

console.log(total);

total /= 10;

console.log(total);

// Math Functions: 

// V. Square Root: 
// 9. Declare a variable number and assign a positive numerical value to it. 

let num = 16;

// 10. Use the Math.sqrt() function to find the square root of number.

console.log(Math.sqrt(num));

// VI. Rounding Numbers: 
// 11. Declare a variable decimalNumber and assign it a decimal value.

let decimalNumber = 3.14;

// 12. Use the Math.round(), Math.floor(), and Math.ceil() functions to round decimalNumber to the nearest whole number. 

console.log(Math.round(decimalNumber));

console.log(Math.floor(decimalNumber));

console.log(Math.ceil(decimalNumber));

// Mixing Operators: 

// VII. Complex Calculation: 
// 13. Declare three variables: a, b, and c with numerical values. 

let a = 15;

let b = 10;

let c = 5;

// 14. Perform a calculation that involves addition, subtraction, multiplication, and division using these variables. 

let result = (a + b) * c - (a / b);

console.log(result);

// VIII. Using Parentheses: 
// 15. Declare three variables: x, y, and z with numerical values. 

let x = 5;

let y = 10;

let z = 15;

// 16. Create a calculation that uses parentheses to control the order of operations. 

let controlResult = (x + y) * z - (z / x) + y;

console.log(controlResult);

// Comparison Operators: 

// IX. Comparing Numbers: 
// 17. Declare two variables, numA and numB, with numerical values. 

let numA = 100;

let numB = 50;

// 18. Use comparison operators (>, <, >=, <=, ===, !==) to compare numA and numB, and log the results. 

console.log(numA > numB);

console.log(numA < numB);

console.log(numA >= numB);

console.log(numA <= numB);

console.log(numA === numB);

console.log(numA !== numB);

// X. Equality and Identity: 
// 19. Declare two variables, value1 and value2, with different types of values (e.g., number and string). 

let valueOne = 10;

let valueTwo = '10';

// 20. Use equality (== and !=) and identity (=== and !==) operators to compare the values and log the results. 

console.log(valueOne == valueTwo);

console.log(valueOne != valueTwo);

console.log(valueOne === valueTwo);

console.log(valueOne !== valueTwo);